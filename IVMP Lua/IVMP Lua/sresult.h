#ifndef __SRESULT_H
#define __SRESULT_H
#include <WinSock2.h>
#include <mysql.h>
/*
class MySQL_Result
{
	friend class MySQL;

private:
	MySQL_Result( MYSQL_RES* res );

public:
	~MySQL_Result( );
	const bool		Empty( );

	MYSQL_ROW		FetchRow( );
	unsigned int	FieldSeek( unsigned int offset );
	MYSQL_FIELD*	FetchField( );

private:
	MYSQL_RES* m_result;
};
*/

#endif